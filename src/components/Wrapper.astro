---
const { title } = Astro.props;
---

<section>
  {title && <h2>{title}</h2>}
  <slot />
</section>

<style>
  section {
    max-width: 44rem;
    margin: 0 auto 0.75rem;
  }

  h2 {
    margin-bottom: 0.5rem;
    font-weight: 700;
    line-height: 1.25;
    font-size: 1.5rem;
  }
</style>
